{
  "name": "@goddard-ai/daemon-client",
  "version": "0.1.0",
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts",
      "source": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tree-run build && tsdown",
    "build:types": "tsdown --no-minify --clean --unused",
    "lint": "oxlint",
    "fmt": "oxfmt",
    "typecheck": "tree-run build:types && tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@goddard-ai/ipc": "workspace:*",
    "@goddard-ai/schema": "workspace:*",
    "@goddard-ai/storage": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^25.3.5"
  }
}
