# AI Agent Instructions

## Daemon IPC

- When adding or removing daemon IPC server methods, update `../../app/src/daemon-ipc-test-handlers.ts` in the same change so the app bridge test stub stays aligned with the daemon server contract.
