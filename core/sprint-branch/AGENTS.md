# sprint-branch Agent Notes

- Before answering conceptual questions about `sprint-branch` procedures or
  changing procedure docs, read `procedures/README.md`.
- Keep procedure documentation durable and conceptual: describe what is
  supported, what changes, and why guardrails exist. Avoid implementation
  walkthroughs.
- When adding, removing, renaming, or changing the meaning of a command, update
  the relevant file in `procedures/commands/` and the index in
  `procedures/README.md`.
