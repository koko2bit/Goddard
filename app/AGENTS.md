# App Agent Notes

- This file is for app-local agent bootstrap rules, not app project policy.
- You may update this file when a human gives durable feedback that future agents working in `app/` should follow.
- Keep this file short, generic, and limited to app-local agent instructions.
- Do not put project plans, product behavior, package-specific API notes, or arbitrary file references here.
- The only sanctioned file references here are `app/CONTRIBUTING.md`, `app/best-practices.md`, and `app/glossary.md`.
- Read `app/CONTRIBUTING.md` before substantial work in `app/`.
- When guidance outgrows this file, move the detail into one of the sanctioned docs instead of expanding this file indefinitely.
