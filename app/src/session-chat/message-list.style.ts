import { css } from "@goddard-ai/styled-system/css"

export default {
  loading: css({
    display: "grid",
    minHeight: "100%",
    placeItems: "center",
    color: "muted",
    fontSize: "0.94rem",
    letterSpacing: "0.01em",
    pointerEvents: "none",
  }),
}
